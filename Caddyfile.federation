# Caddyfile for Federation Registry
# Configured for HTTP-only access via IP address
# No SSL/domain required - suitable for internal networks or initial testing

:80 {
    reverse_proxy federation:5180
}
